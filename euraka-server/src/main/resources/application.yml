server:
  port: 8081
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:8081/eureka/
spring:
  application:
    name: eurka-server